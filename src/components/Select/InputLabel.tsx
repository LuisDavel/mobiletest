import { TextInputProps, TextInput } from 'react-native';

import * as S from './styles';

type InputProps = {
  label: string
} 

export default function InputLabel({ label }: InputProps) {
  
  return ( 
   <></>
  )
}